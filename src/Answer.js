export default class Answer {
    constructor(id, label, evaluationText, consequence) {
        this.id = id;
        this.label = label;
        this.evaluationText = evaluationText;
        this.consequence = consequence;
    }
}
